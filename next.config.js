/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
    images: {
      domains: ['github.com', 'user-images.githubusercontent.com','source.unsplash.com'],
    },
  };